/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["localhost", "seu-dominio.com"], // Adicione os domínios necessários
    },
};

export default nextConfig;
